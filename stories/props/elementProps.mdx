import { Canvas, Story } from '@storybook/blocks'

# elementProps

- type: `object`

Props passed to main calendar `<div>`.

**Note:** Will not pass down `className`.

<Story id="props--element-props" />
